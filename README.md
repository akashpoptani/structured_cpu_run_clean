# structured_cpu_run_clean

This directory is a supervised clean migration lane for the CPU DeepSeek run stack.

The original `structured_cpu_run` directory remains the source of truth.

This lane is not production yet.

No giant rewrites are allowed.
